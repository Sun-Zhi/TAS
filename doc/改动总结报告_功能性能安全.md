# 任务分配系统 · 改动总结报告（功能 / 性能 / 安全）

> 说明：本报告仅梳理代码改动，未改动任何系统代码、未提交。覆盖两部分：
> 1. 最近一次提交 `eaed5ed`（"修改了一些bug，提高了一下安全性"，30 文件，+4070/−2273）
> 2. 当前未提交的工作区改动（8 个应用源码文件，+74/−20，外加少量测试脚本）
>
> 归属说明：应用源码（server.js / src/ / public/）的未提交改动均为你（sunzhi）自己所做；scripts/ 下的测试脚本调整是本轮为搭建隔离测试环境临时加的辅助脚本（非应用运行代码），可按需丢弃。

---

## 一、最近一次提交 eaed5ed（"修改了一些bug，提高了一下安全性"）

### 功能 (Functional)
- **前端架构重构**：`app.js` 从约 2276 行的单体大幅拆解为模块化经典脚本，新增 `util.js`、`modal.js`、`due-picker.js`、`task-actions.js`、`users.js`、`login.js` 等，可维护性显著提升。
- **自定义日期时间选择器** `due-picker.js`（新增 549 行）：替代浏览器默认 `datetime-local`，统一截止时间选择体验。
- **登录与用户体系重写**：`login.js` 新增、`authRoutes.js` 重写（+262/−）、`userRoutes.js` 重写（+188），支持角色化登录、会话与令牌管理。
- **任务/统计路由增强**：`taskRoutes.js`（+123）、`statsRoutes.js`（+34）重写部分逻辑。
- **运维与回归脚本**：新增 `reset.js`、`reset-regression.js`、`seed-demo.js`、`scryptGate.js`（登录校验闸门）等。

### 性能 (Performance)
- **数据库索引优化**（`src/db.js`，提交内 +115 行）：新增/调整 `idx_tasks_completed`、`idx_tasks_due`、`idx_att_task`、`idx_log_task` 等索引，加速任务与附件/日志查询。
- **统一校验闸门** `scryptGate.js`：集中密码校验，配合常量成本策略（同时有安全收益）。

### 安全 (Security)
- 提交主旨即为安全加固：`src/auth.js`（+34）密码校验、SCRYPT 参数、会话/令牌机制改进。
- 新增 `scryptGate.js`：登录校验统一走常量成本的 scrypt 闸门，缓解暴力破解与计时侧信道。
- 清理：删除了上次误提交的临时文件 `public/js/task-actions.js.tmp.27732...`。

---

## 二、当前未提交改动（工作区）

### 功能 (Functional)
1. **密码"显示/隐藏"切换**
   - `public/index.html`：用户表单密码框由明文 `type="text"` 改为 `type="password"`，并包进 `.pwd-row`，新增"显示/隐藏"按钮 `btnToggleUfPwd`。
   - `public/js/users.js`：新增 `resetPasswordField()`（每次打开弹窗复位为掩码，避免上次状态残留）+ 按钮点击切换监听（含 `aria-pressed` 无障碍状态）。
   - `public/css/style.css`：新增 `.pwd-row` 弹性布局样式。
   - **为什么**：原实现把密码明文显示，存在旁观泄露风险；新交互让管理员确认临时密码时可临时查看，平时默认掩码。
2. **任务编辑"原执行人已停用"修复**
   - `public/js/task-actions.js`：打开编辑框时先清除上次遗留的 `data-inactive-placeholder` 占位选项（否则会累积、污染下拉并让"重新派发"分支误命中残留占位）；若原执行人已停用/改角色（不在启用下拉里），追加一个 `disabled` 的"（已停用）"占位项保留原 `assignee_id`。
   - `src/routes/taskRoutes.js`：服务端兜底——重新派发时即使执行人未变化，也要校验当前执行人仍为"启用中的执行者"，否则返回 400，防止退回任务经 API 保持原 `assignee_id` 绕过前端重选、继续挂在已停用账号上。
   - **为什么**：原逻辑中，已停用执行人的任务打开编辑后 select 的 value 会被静默清空，保存时提交 `assignee_id=0` 被服务端 400 拒绝，导致"只改描述/截止时间"也被卡死；同时退回任务可被绕过重选校验。
3. **`autocomplete="new-password"`**（`index.html` 密码框）：避免浏览器自动填充/缓存管理员口令。
4. **缓存刷新**：`index.html` / `login.html` 的 CSS 版本号、`screen.html` 的 `screen.js` 版本号 bump（强制客户端刷新静态资源）。

### 性能 (Performance)
1. **会话清理索引**（`src/db.js`）：新增 `idx_sessions_expires ON sessions(expires_at)`。登录成功路径会按 `expires_at` 清理过期会话，索引避免每次登录对 sessions 表全表扫描。
2. **种子哈希缓存**（`src/db.js`）：同一次初始化中，演示账号共享口令只计算一次 scrypt（`hashForSeed` + `seedHashCache` Map）并复用哈希串；首次启动的同步 scrypt 从最多 6 次降到 2 次。
3. **DUMMY_HASH 预置**（`src/db.js`）：`DUMMY_HASH` / `DUMMY_HASH_LEGACY` 改为直接预置固定形态（salt/digest 占位），不再在启动时真实执行 scrypt 生成随机值，**省去每次启动约 1 秒的同步开销**；且登录校验路径成本与真实哈希完全一致（不牺牲安全）。

### 安全 (Security)
1. **防账号枚举（计时攻击）维持恒定成本**（`src/db.js`）：登录路径保持"真实校验 + 一次固定成本 dummy"共 2 次 scrypt、总成本恒定，消除跨账号/跨哈希格式的登录延迟差。`DUMMY_HASH` 预置后仍为"格式可解析、参数安全"，安全等级不变。
2. **管理员口令独立哈希**（`src/db.js`）：管理员口令不纳入 seed 复用缓存，独立 `hashPassword`。避免 `ADMIN_PASSWORD` 与演示口令巧合相同时，两个账号哈希串完全一致而泄露"口令相同"的事实。
3. **`autocomplete="new-password"`**（见功能 3）：降低浏览器自动填充/缓存口令带来的泄露风险。
4. **日志级别调整**（`server.js`）：启动/关闭/种子初始化日志由 `console.log` 改为 `console.warn`。**无安全影响**，仅为在生产环境区分启动信息与常规输出（便于日志分级/监控）；属于日志行为变化，不是功能或安全加固。

### 其他（非应用运行代码）
- `scripts/auth-regression.js`、`scripts/task-security-regression.js`、`scripts/ui-test.js`：小幅测试脚本调整（本轮 UI 测试临时搭建隔离环境所用，**非应用运行代码**，可丢弃）。
- `.gitignore`：+2 条忽略规则。
- 删除 `public/js/task-actions.js.tmp.27732...`（清理上次提交误带的临时文件）。

---

## 三、建议
- 上述应用源码改动（server.js / src / public）均为你的工作，若确认无误可提交；我未做任何系统代码改动、未提交。
- 本轮为做隔离 UI 测试（12345 端口）临时产生的 `scripts/` 测试脚本调整与临时文件 `C:\Users\sunzhi\AppData\Local\Temp\ui-e2e-test.js`，不在应用运行路径上，可按需删除或保留为回归用例。
- 若希望我在提交前再跑一遍回归（不影响生产、不改动代码），告知即可。
