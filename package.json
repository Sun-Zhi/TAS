{
  "name": "taskassign",
  "version": "1.0.0",
  "description": "任务分配系统 - 多角色任务派发、执行跟踪与数据大屏",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "reset": "node scripts/reset.js",
    "test": "node scripts/auth-regression.js && node scripts/utils-regression.js && node scripts/reset-regression.js && node scripts/task-security-regression.js && node scripts/e2e-test.js"
  },
  "engines": {
    "node": ">=22.5.0"
  },
  "dependencies": {
    "express": "^4.21.2",
    "multer": "^2.0.2"
  }
}
